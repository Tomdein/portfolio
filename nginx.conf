server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing — all routes fall back to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # YAML content files — public GET, auth-protected PUT
    location /content/ {
        root /usr/share/nginx/html;

        # GET — public, no auth
        limit_except PUT {
            allow all;
        }

        # PUT — requires Basic Auth + DAV
        dav_methods PUT;
        auth_basic "Admin";
        auth_basic_user_file /etc/nginx/.htpasswd;

        client_body_temp_path /tmp/nginx;
        create_full_put_path on;
    }

    # Cache static assets aggressively
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Cache images — public GET, auth-protected PUT / DELETE / MOVE
    location /images/ {
        client_max_body_size 20M;
        root /usr/share/nginx/html;

        # Directory listing for image manager (returns JSON array)
        autoindex on;
        autoindex_format json;

        # Cache headers for GET (applied outside limit_except so they apply to public reads)
        expires 30d;
        add_header Cache-Control "public";

        # Write operations — require Basic Auth + DAV
        limit_except GET HEAD {
            auth_basic "Admin";
            auth_basic_user_file /etc/nginx/.htpasswd;
        }

        dav_methods PUT DELETE MOVE;
        client_body_temp_path /tmp/nginx;
        create_full_put_path on;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Gzip
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml application/x-yaml;
    gzip_min_length 256;
}
